{
  "description": "Configuration for RPC regression testing V2",
  "defaultTolerance": 0.001,
  "methodTolerances": {
    "circles_getTotalBalance": 1.0,
    "circlesV2_getTotalBalance": 1.0,
    "circles_getTokenBalances": 1.0,
    "circlesV2_findPath": 1.0,
    "circles_query": 0.05
  },
  "normalizeFields": [
    "timestamp",
    "blockNumber",
    "transactionIndex",
    "logIndex",
    "transactionHash",
    "lastUpdatedAt",
    "crc",
    "circles",
    "staticCircles"
  ],
  "expectedFailures": [
    "circles_getNetworkSnapshot",
    "circles_events (filter: blockNumber < 100 OR blockNumber > 40000000)",
    "circles_events (filter: blockNumber > 38000000 AND blockNumber < 39000000)",
    "circles_events (filter: combined with test addr3 and block range)",
    "circles_events (filter: nested AND/OR with test addr3)",
    "circles_events (filter: transactionHash IS NOT NULL)",
    "circles_getAggregatedTrustRelationsEnriched (for: test addr1)",
    "circles_getProfileView (for: test addr1)",
    "circles_getTransactionHistoryEnriched (for: test addr1)",
    "circles_getTrustNetworkSummary (for: test addr1)",
    "circles_getValidInviters (for: test addr1)",
    "circles_searchProfileByAddressOrName (query: test prefix)",
    "circles_getAggregatedTrustRelationsEnriched (for: test addr2)",
    "circles_getProfileView (for: test addr2)",
    "circles_getTransactionHistoryEnriched (for: test addr2)",
    "circles_getTrustNetworkSummary (for: test addr2)",
    "circles_getValidInviters (for: test addr2)",
    "circles_searchProfileByAddressOrName (query: random addr)",
    "circles_getAggregatedTrustRelationsEnriched (for: test addr3)",
    "circles_getProfileView (for: test addr3)",
    "circles_getTransactionHistoryEnriched (for: test addr3)",
    "circles_getTrustNetworkSummary (for: test addr3)",
    "circles_getValidInviters (for: test addr3)",
    "circles_searchProfileByAddressOrName (query: 0x)"
  ],
  "subscriptionTest": {
    "enabled": false,
    "duration": 60,
    "minEvents": 3,
    "filterAddress": null
  },
  "comments": {
    "defaultTolerance": "Default tolerance in percentage for numeric comparisons (0.001 = 0.001%)",
    "methodTolerances": "Method-specific tolerances override the default. Balance methods need higher tolerance due to time-based calculations.",
    "normalizeFields": "Fields to remove before comparison. Includes dynamic data (timestamp, blockNumber) and lossy decimal convenience fields (crc, circles, staticCircles) that are derived from precise atto values",
    "expectedFailures": "Tests that are expected to fail (e.g., network state, new features not in production)",
    "subscriptionTest": "Optional WebSocket subscription testing. Exits when minEvents are received OR duration seconds elapse (whichever comes first). filterAddress=null receives ALL events (recommended). Set enabled=true to test real-time subscriptions (local endpoint only)."
  }
}
