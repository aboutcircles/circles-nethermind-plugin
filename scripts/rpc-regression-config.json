{
  "description": "Configuration for RPC regression testing",
  "defaultTolerance": 0.001,
  "methodTolerances": {
    "circles_getTotalBalance": 0.1,
    "circlesV2_getTotalBalance": 0.1,
    "circles_getTokenBalances": 0.1,
    "circlesV2_findPath": 0.1,
    "circles_query": 0.05
  },
  "normalizeFields": [
    "timestamp",
    "blockNumber",
    "transactionIndex",
    "logIndex",
    "transactionHash",
    "lastUpdatedAt"
  ],
  "expectedFailures": [
    "circles_getNetworkSnapshot",
    "circles_getAggregatedTrustRelationsEnriched (addr1)",
    "circles_getAggregatedTrustRelationsEnriched (addr2)",
    "circles_getAggregatedTrustRelationsEnriched (addr3)",
    "circles_getProfileView (addr1)",
    "circles_getProfileView (addr2)",
    "circles_getProfileView (addr3)",
    "circles_getTransactionHistoryEnriched (addr1, recent)",
    "circles_getTransactionHistoryEnriched (addr2, with limit)",
    "circles_getTransactionHistoryEnriched (addr3, block range)",
    "circles_getTrustNetworkSummary (addr1)",
    "circles_getTrustNetworkSummary (addr2)",
    "circles_getTrustNetworkSummary (addr3, with maxDepth)",
    "circles_getValidInviters (addr1, default min)",
    "circles_getValidInviters (addr2, min 96)",
    "circles_getValidInviters (addr3, min 50)",
    "circles_getInvitationOrigin (addr1)",
    "circles_getInvitationOrigin (addr2)",
    "circles_getInvitationOrigin (addr3)",
    "circles_searchProfileByAddressOrName (by address prefix)",
    "circles_searchProfileByAddressOrName (by address)",
    "circles_searchProfileByAddressOrName (by text)",
    "circles_events (filter: blockNumber < 100 OR blockNumber > 40000000)",
    "circles_events (filter: blockNumber > 38000000 AND blockNumber < 38001000)",
    "circles_events (filter: combined with test addr3 and block range)",
    "circles_events (filter: nested AND/OR with test addr3)",
    "circles_events (filter: transactionHash IS NOT NULL)"
  ],
  "subscriptionTest": {
    "enabled": true,
    "duration": 90,
    "minEvents": 1,
    "filterAddress": "0x4b6f72008e7aca33de36b6565ef30264626b21db"
  },
  "comments": {
    "defaultTolerance": "Default tolerance in percentage for numeric comparisons (0.001 = 0.001%)",
    "methodTolerances": "Method-specific tolerances override the default. Balance methods use 5% tolerance to accommodate real-time inflationary calculations between staging and production.",
    "normalizeFields": "Fields to remove before comparison. Includes dynamic data (timestamp, blockNumber, lastUpdatedAt) that inherently differ between systems.",
    "expectedFailures": "Tests that are expected to fail (e.g., network state, new features not in production)",
    "subscriptionTest": "Optional WebSocket subscription testing. Exits when minEvents are received OR duration seconds elapse (whichever comes first). filterAddress=null receives ALL events (recommended). Set enabled=true to test real-time subscriptions (local endpoint only)."
  }
}